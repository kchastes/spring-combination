<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 可以使用name添加别名用' ',;进行分割 -->
    <!-- 这种方式都是默认的构造方法 -->
    <bean id="color" class="chaste.core.simple.BlackColor">
    </bean>

    <!-- 还可以使用静态方法 -->
    <bean id="colorStatic" class="chaste.core.simple.BlackColor" factory-method="getInstance">
    </bean>

    <!-- 实例工厂方法创建 -->
    <bean id="colorFactory" class="chaste.core.simple.factory.ColorFactory"/>
    <bean id="colorFactoryMethod" factory-bean="colorFactory" factory-method="blackColor"/>
</beans>